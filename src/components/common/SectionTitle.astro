---
interface Props {
  as?: 'h1' | 'h2' | 'h3' | 'h4';
  class?: string;
}

const { as: Tag = 'h2', class: className = '' } = Astro.props;
---

<Tag
  class:list={[
    'font-serif font-medium leading-tight tracking-tight text-balance',
    className,
  ]}
>
  <slot />
</Tag>
